from django.apps import AppConfig


class AttendanceConfig(AppConfig):
    name = 'apps.attendance'
    default_auto_field = 'django.db.models.BigAutoField'
    verbose_name = 'Gestion des Presences'
